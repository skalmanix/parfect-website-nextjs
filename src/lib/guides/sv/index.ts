import type { Guide } from "../types";
import { GUIDES_EN } from "../en";

/** Swedish guides — translated content with same slugs for hreflang parity. */
export const GUIDES_SV: Guide[] = GUIDES_EN;
